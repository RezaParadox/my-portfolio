{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website - React client + Express API",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --filter client build",
    "start": "pnpm --filter server start"
  }
}
